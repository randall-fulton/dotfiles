# System-wide Copilot Instructions

- All repositories use `jj` (Jujutsu) for version control instead of `git`. Use `jj` commands for all VCS operations (e.g. `jj log`, `jj diff`, `jj describe`, `jj new`, `jj squash`, etc.).
